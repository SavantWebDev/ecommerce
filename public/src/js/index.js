function linksMenu() {
  const linkMenu = document.querySelectorAll(".link-menu");
  const svgMenu = document.getElementById("svgMenu");
  linkMenu.forEach((e) => {
    linkMenu.addEventListener("", listener, options);
  });
}
